const runValidation() {
  const { email, password } = this.state;
  let isInvalid;

  if() {
    isInvalid = true;
  } else if() {
    isInvalid = true;
  } else {
    isInvalid = false;
  }

  return isInvalid;
}

export default runValidation();
